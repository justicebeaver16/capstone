{
  "name": "capstone-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:render": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && npm run init:schema --prefix backend && npm run reset --prefix backend"
  }
}